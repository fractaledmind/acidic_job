# frozen_string_literal: true

RSpec.describe AcidicJob do
  it "has a version number" do
    expect(AcidicJob::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
